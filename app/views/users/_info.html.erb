<section class="user_info">
  <%= gravatar_for @user %>
  <h1><%= link_to @user.name, @user, title: t(".view_my_profile") %></h1>
  <p>
    <span><%= raw t(".microposts", count: @user.microposts.size) %></span>
  </p>
</section>
<section class="stats">
  <%= render "shared/stats" %>
</section>
